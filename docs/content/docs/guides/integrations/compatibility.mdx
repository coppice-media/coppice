---
title: Compatibility and integration status
description: Evidence-labelled compatibility for Coppice protocols, clients, devices, and providers
---

This is the human-facing status page for Coppice integrations. It separates
what is implemented from what has been exercised in a real app or device. A
route, DTO, or successful server replay is not by itself proof that every client
works.

For route-level details and pinned source references, see [client
verification](/docs/developer/client-verification), [platform
status](/docs/developer/platforms), and [protocol standards](/docs/developer/standards).

## Evidence labels

[Integration architecture](/docs/developer/integration-architecture) defines
the canonical status vocabulary. The detailed client versions, evidence
records, and exact tested flows are maintained only in the
[client verification matrix](/docs/developer/client-verification).

## Client and protocol matrix

| Surface or client | Implementation | Evidence | What the evidence means |
| --- | --- | --- | --- |
| Komga profile / Komelia | Shipped | Device-tested; Contract-tested | Komelia 0.19.0 was device-tested; current 0.19.3 source is a separate Source-only record. The profile is not full Komga parity. |
| Built-in Komf-compatible API / Komelia | Shipped | Contract-tested; Source-only | Komf 2.0.1 route, DTO, and device-permission behavior is tested; no live Komelia 0.19.3 device run is claimed. |
| Grimmory `/komga` alias | Shipped | Device-tested; Contract-tested | Device login and selected listing, file, and thumbnail routes were exercised. |
| Mihon Komga extension and tracker | Shipped | Device-tested; Contract-tested | Tracker source-analysis pin `21af65b1` and replay-client v0.20.4 pin `df650725` are distinct; evidence does not imply other clients. |
| Kavita profile / Turnleaf and Kamigura | Shipped | Device-tested; Contract-tested | Device runs are scoped to Turnleaf and Kamigura; other Kavita-shaped clients remain Source-only. |
| OPDS 1.2/PSE | Shipped | Device-tested; Contract-tested | Liseur exercised OPDS 1.2; generic OPDS readers are not blanket-certified. |
| OPDS 2.0 | Shipped | Contract-tested | Server contract only; Liseur has no OPDS 2 parser. |
| Kobo sync and KEPUB | Shipped | Contract-tested; Blocked | Routes and KEPUB conversion are contract-tested; no physical Kobo run is recorded. KEPUB's 35/35 parity sample is distinct from the 36-book/37 MB performance corpus. |
| KOReader/KOSync | Shipped | Contract-tested; Device-tested; Source-only | Liseur's KOReader-sync flow is device-tested. The native KOReader app and `coppice.koplugin` have not been loaded; the plugin is Source-only at v2026.07.1 `9192014d`. |
| liseur-sync | Shipped | Device-tested; Contract-tested; Source-only | Existing device flows cover positions/heads. Current Liseur v0.19.0 `62ecb5a5` and liseur-sync `906889ff` pins are Source-only; attachment routes/storage have source/contract evidence, not an attachment device test. |
| Audiobookshelf REST / Lissen | Shipped | Contract-tested; Source-only | Contract replay is pinned to Lissen 1.11.22; current Lissen 1.12.5 `fd5c0417` is Source-only. No Lissen device run is claimed. |
| Official Audiobookshelf app | Shipped profile | Device-tested; Source-only | Android 35 test of app v0.14.0-beta is scoped, not full parity. Current source pin v0.14.1-beta `2ac4de4d` is Source-only, not a device-tested version. |
| Ordinary EPUB playback | Shipped | Device-tested | EPUB reading is separate from audiobook synchronization. |
| Ordinary audiobook playback | Shipped | Device-tested | Audiobook playback is separate from EPUB reading and alignment. |

This summary is intentionally compact. The [client verification matrix](/docs/developer/client-verification)
is the canonical record for dates, pins, routes, and caveats.

## Provider status

### Hardcover

Hardcover metadata mapping is **Shipped** and **Contract-tested** against
offline payload fixtures. Coppice has not been verified with a live Hardcover
credential; live-credential verification is **Blocked** until a controlled
credential run is available.

Hardcover account, reading progress, and journal synchronization are
**Planned** and currently **Blocked**. They are not implied by metadata lookup,
and no OAuth or account-sync workflow is shipped. See the [Hardcover developer
record](/docs/developer/hardcover-integration) for the exact boundary.

### Storyteller and read-aloud

Pairing, `SyncMapV1`/SMIL timing import, alignment enqueue, deterministic
M4B-to-EPUB streaming, and authenticated cache-only status/download are
shipped. Remote workers can use optional operator-configured Storyteller or
native CTC alignment runners; neither is bundled or run as server-local ML.

Readiness quality checks and full synchronized reader playback/UI remain
planned; ordinary EPUB and audiobook playback remain separate. No ordinary
playback device test is evidence for synchronized read-aloud playback. See
[read-aloud architecture](/docs/developer/read-aloud).

## Setup guides

- [Kobo sync](/docs/guides/integrations/kobo) — server route setup and the
  physical-device limitation.
- [KOReader sync](/docs/guides/integrations/koreader) — API-key setup,
  hash-based matching, and the `ENABLE_KOREADER_SYNC` gate.
- [Komelia Komf API](/docs/guides/integrations/komelia-komf) — device-key URL setup, the metadata-edit opt-in, read-only behavior, and limits.
- [Metadata fetching](/docs/guides/integrations/metadata-fetching) — provider
  configuration and the offline-only Hardcover boundary.
- [Request and acquisition boundary](/docs/guides/integrations/acquisition) —
  metadata-backed requests, approval, and manager-confirmed MAM Bridge handoff.

## Current, planned, and blocked at a glance

| Area | Current | Planned | Blocked |
| --- | --- | --- | --- |
| Library access | OPDS, Komga-shaped, Kavita, Kobo, KOReader, liseur-sync, ABS-compatible REST, ordinary EPUB, and ordinary audiobook paths | — | A client/device not explicitly listed as Device-tested |
| Requests and acquisition | Metadata-backed ledger, permissions, per-user visibility, manager approval/rejection, notifications, social handoff, manager-confirmed MAM Bridge search/grab and staged-ingest handoff | — | MAM Bridge configuration and operator confirmation are required; no in-process downloader, automatic retries, or auto-grab |
| Read-aloud | Pairing, maps, alignment enqueue, cache endpoints, optional configured worker runners | Readiness checks and synchronized playback/UI | Physical synchronized read-aloud evidence |
| Hardcover | Offline-tested metadata mapper | Account/progress/journal sync | Live credential verification and account-sync prerequisites |

The [modular deployment guide](/docs/guides/configuration/modular-deployment)
explains how to compile only the protocol features you intend to run.
