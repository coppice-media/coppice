---
title: Client verification
description: Which real clients have been verified against Coppice, with what evidence, and what is known not to work
---

This page is the single record of client-facing verification. A row never claims
more than its evidence:

- **Device-tested** — a person exercised a real app or device against the
  fixture server (`scripts/dev-fixture-server.sh`, headless debug build, LAN or
  emulator address). Date and app version are given where available.
- **Contract-tested** — a pinned protocol fixture, replay, or server-side probe
  exercised the wire contract. It does not prove that the app works.
- **Source-only** — implementation was checked against pinned client source, but
  no run is claimed.
- **Shipped**, **Planned**, and **Blocked** describe product state rather than
  substituting for client evidence.

Only **Device-tested** counts as “works in the app.” Harness/source evidence
never becomes app/device proof. The snapshot below includes runs through
2026-09-07.
Source pins refreshed after this snapshot are source-analysis only; they do
not extend the date or scope of an older device run.

Current Liseur v0.19.0 (`62ecb5a5`) and liseur-sync (`906889ff`) pins are
source-only; they do not upgrade the older device runs below.

## Matrix

| Client | Protocol / route tree | Login | Browse | Read | Download | Progress sync | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Komelia 0.19.0 (Android) | Komga profile `/api/v1`, `/api/v2`, Readium | Device-tested | Device-tested | Device-tested (CBZ + EPUB) | Device-tested (CBZ + EPUB, offline) | Device-tested (read-progress, mark-read) | **Device-tested** 2026-09-03/04; historical **Contract-tested** `replay` and `replay-readium`. Current Komelia 0.19.3 source pin `3c5f501ef24b3acc239302adcab28efe0772c513` is Source-only; it is not the device-tested version. |
| Liseur (Komga profile) | Komga profile | Device-tested | Device-tested | Device-tested | — | Device-tested | **Device-tested** 2026-09-04 user report |
| Liseur (OPDS 1.2) | `/opds/v1.2` | Device-tested | Device-tested (merged library, covers) | Device-tested | Device-tested | Device-tested via KOReader pairing | **Device-tested** 2026-09-04 user report/screenshot; current v0.19.0 source pin is Source-only. |
| Liseur (OPDS 2.0) | `/opds/v2.0` | — | Blocked (app has no OPDS 2 parser) | Blocked | Blocked | Blocked | **Contract-tested** server catalog only; Liseur `OpdsHttp.kt:93` accepts OPDS 1.2 XML, not `application/opds+json` |
| Liseur (KOReader sync) | `/koreader/{api_key}` | Device-tested | — | — | — | Device-tested | **Device-tested** 2026-09-04 user report; **Contract-tested** server probe `users/auth` 200; current Liseur v0.19.0 pin is Source-only. |
| Coppice KOReader plugin | OPDS 2.0, KOSync, liseur-sync | Source-only | Source-only | Source-only | Source-only | Source-only | **Source-only** against KOReader v2026.07.1 (`9192014d`); it has not been loaded into the native app. |
| Liseur (liseur-sync) | `/v1/*` | Device-tested | Device-tested | Device-tested | Device-tested | Device-tested (positions/heads) + Contract-tested changes, annotations, catalog | **Device-tested** 2026-09-04 recorded client flows; current Liseur/liseur-sync pins are source-only. Attachment routes/storage ship with source/contract evidence but have no attachment client test. |
| Grimmory alias | `/komga/api/*` | Device-tested (login) | Contract-tested | Contract-tested | Contract-tested | Contract-tested | **Device-tested** via Liseur login; server route contract replayed |
| Mihon / Tachiyomi | Komga profile | Device-tested (API key and user/pass) | Device-tested + Contract-tested | Device-tested + Contract-tested | Device-tested | Device-tested + Contract-tested tracker | **Device-tested** 2026-09-04 retest; historical `replay-mihon` uses replay client v0.20.4 `df650725`. Mihon's Komga tracker source-analysis pin `21af65b1` is a distinct record. |
| Komf 2.0.1 (external sidecar) | Komga media-server adapter (Basic + SSE) | — | Contract-tested (historical direct `/api/v1`) | — | — | Contract-tested metadata writes + SSE | **Source-derived** pin `5ad67370`; **Contract-tested** historical direct `/api/v1` replay. Pinned sidecar smoke is separately scoped to `/api/komga/media-server/connected` and `/api/komga/media-server/libraries`. |
| Kavita profile | `/api/*` Kavita routes, `x-api-key` + `Plugin/authenticate` JWT | Contract-tested | Contract-tested (`Series/all-v2` + `Pagination`) | Contract-tested (`Reader/image`) | — | Contract-tested (`get-progress`/`progress`) | Historical Contract-tested `replay-kavita`; DTO comparison uses Kavita 0.9.1.4. |
| Turnleaf (Kavita) | Kavita profile | Device-tested | Device-tested | Device-tested | Device-tested | Device-tested | **Device-tested** 2026-09-06 |
| Kamigura (Kavita) | Kavita profile | Device-tested | Device-tested | Device-tested (MangaDex virtual image lane) | Device-tested | Device-tested (want-to-read) | **Device-tested** 2026-09-06; EPUB reader lane is intentionally absent |
| Inkita, Kover, Kamare, Kareadita, Mihon Kavita tracker | Kavita profile | Source-only | Source-only | Source-only | Source-only | Source-only | **Source-only** pinned source; no device run claimed |
| Kobo device | `/kobo/{api_key}` | Contract-tested (`initialization` 200) | Contract-tested (`library/sync`) | Contract-tested (KEPUB delivery, `Range`) | Contract-tested | Contract-tested (`ReadingState` round-trip) | **Contract-tested**; **Blocked** for physical-device evidence. KEPUB byte parity is 35/35 on its real-book sample; the separate performance corpus has 36 books/37 MB. |
| Lissen `1.11.22-release` | Audiobookshelf profile | Contract-tested | Contract-tested | Contract-tested (listen) | Contract-tested | Contract-tested | **Contract-tested** chained replay for 1.11.22; no Lissen device run. Current Lissen 1.12.5 pin `fd5c0417` is Source-only, not this replay. |
| Official Audiobookshelf app `v0.14.0-beta` | Audiobookshelf profile | Device-tested (scoped) | Device-tested (scoped) | Device-tested (seek drag) | Device-tested (one-track download) | Source-only | **Device-tested** Android 35 `test_35` emulator, 2026-09-07; scoped run, not complete ABS parity. Current app source pin v0.14.1-beta `2ac4de4d` is Source-only. |

## Historical resource observation

`14.5 MB` RSS idle and `110 MB` peak during a parallel KEPUB batch were
recorded from a debug build after five hours of uptime. This is a historical
machine observation, not compatibility proof or a production capacity
guarantee. It is separate from the app/device rows above.

## How a device report is triaged

1. Capture the request path, method, status, and relevant client headers from
   the test environment.
2. Reproduce the request against the same protocol profile and compare it with
   the pinned client contract.
3. Fix at the adapter boundary and add a focused contract test for the observed
   behavior.
4. Update this matrix with the exact client version, exercised flow, date, and
   remaining boundary; do not broaden an older device result.

## Known client-side limits (not Coppice bugs)

- Liseur has no OPDS 2 parser; use the OPDS 1.2 URL.
- Komelia's Readium DTOs are stricter than the Readium spec (`rel` must be a
  string, `published` a calendar date, contributors lists); the Komga alias
  normalizes to those types and native routes keep the spec shapes.
- The `replay-readium` spec needs a book with no existing reading session for
  its `PUT /progression`; on a book the phone has opened it returns `409` by
  design (older timestamp than the live session).
- Kamigura's EPUB image request returns 404 because that client is an image
  reader; Turnleaf/Inkita/Kover are the EPUB-capable Kavita routes in the pinned
  inventory.
- The official Audiobookshelf app's tap-only seek and stale/duplicate download
  queue are client-side interaction states. A clean seek drag and one-track
  download were **Device-tested**; see [Audiobookshelf profile](/docs/developer/abs-compat).
- Kobo stock firmware does not render WebP in CBZ or KEPUB. The optional comic
  transform is shipped but disabled by default; no physical Kobo run is
  recorded. See [clients](/docs/developer/clients).

## Evidence links

- [Integration architecture](/docs/developer/integration-architecture) —
  taxonomy and evidence semantics.
- [Komga compatibility](/docs/developer/komga-compat) — Komelia/Mihon/Grimmory
  contract and route boundaries.
- [Kavita compatibility](/docs/developer/kavita-compat) — profile contract;
  device rows above remain scoped to the named clients.
- [Audiobookshelf profile](/docs/developer/abs-compat) — Lissen and official app
  route evidence, including the scoped emulator run.
- [Kobo sync capabilities](/docs/developer/kobo-sync-capabilities) — protocol
  and format limits.
