These fixtures describe only the observable wire contracts used by the
focused protocol test. They are not MAM or qBittorrent source and contain no
real credentials. The test server serves search JSON and a bounded torrent
artifact, records the gateway-held cookie, accepts one tagged multipart add,
and reports a qBittorrent completion state with a regular EPUB under the
configured handoff root.
